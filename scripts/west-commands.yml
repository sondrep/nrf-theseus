# Keep the help strings in sync with the values in the .py files!
west-commands:
  - file: scripts/west_commands/build.py
    commands:
      - name: build
        class: Build
        help: configure and build a Theseus application

  - file: scripts/west_commands/flash.py
    commands:
      - name: flash
        class: Flash
        help: flash firmware to target using nrfutil
